# Runtime dependencies. Development tools live in requirements-dev.txt.
#
# Upper bounds stop a major release arriving unannounced. There is no lock
# file, so these bounds are what makes an install reproducible.
python-decouple>=3.8,<4
transmission-rpc>=7.0,<8
